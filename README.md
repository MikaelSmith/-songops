# Karaoke Kompanion - a Karaoke Kontrol for your iPhone

## Build

Uses Pod, so `gem install pod`, then `pod install`, and open `Karaoke Kompanion.xcworkspace` in Xcode. Currently expects Xcode 7 Beta 5.
