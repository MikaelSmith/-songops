# #songops - a Karaoke Control for your iPhone

## Build

Uses Pod, so `gem install pod`, then `pod install`, and open `#songops.xcworkspace` in Xcode 7.

### Note:

This will not build in Xcode 10.2 or higher.
